<!DOCTYPE html>
<html lang="en">
    <head>
        <title>Login</title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">                 
         
        <style type="text/css"> 
        :root{
            --bg1 :#51c1b6;
            --bg2 :#7eb969;
            --text :#7eb969;
        } 
        *{
            margin: 0;
            padding: 0;
            outline: none;
        }
        
        .container{
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            width: 215vh;
            height: 100vh;
            background: linear-gradient(to bottom right,var(--bg1),var(--bg2));
        }
        .form-login{
            width: 350px;
            height:  450px;
            background: white;
            border-radius: 10px;
            padding: 40px 30px;
        }
        .form-login h1{
            margin-bottom: 40px;
        }
        .form-login button{
            height: 35px;
            width: 100%;
            margin-bottom: 30px;
            background: linear-gradient(120deg,var(--bg1),var(--bg2),var(--bg1));
            border: none;
            color: rgb(255, 255, 255);
            padding-left: 12px;
            border-radius: 5px;
            background-size: 200%;
            transition: 0.5s;
        }
        .form-text{
            margin-bottom: 30px;
            text-align: left;
        }
        .form-text input{
            width: 100%;
            height: 35px;
            border: none;
            border-bottom: 2px solid #ccc;
            cursor: pointer;
        }
        .form-text label{
            font-weight: 700;
        }

        </style>  
    </head>
    <body>
        
        <div class="container">
             <div class="form-login"> 
                <h1> Welcome Group 9</h1>
                <div class="form-text">
                    <label for = "uname">USER NAME</label>
                    <input type="text" placeholder="Enter Username" name="u" id="un" >
                    <!-- name = "hoanganhubqn" required -->
                </div>
                <div class="form-text">
                    <label for = "psw">PASSWORD</label>
                    <input type="password" placeholder="Password"  name="p" id="pw" >
                    <!-- name = "a" required -->
                </div>
                <!-- <a  href ="/windpower"></a> -->
                <button onclick= check()>LOGIN</button>
             </div>
        </div>
    </body>

<script>
    var dn,mk
    socket.on("server-update-login", function (datalogin) {
        dn= datalogin.tendangnhap;
        mk= datalogin.matkhau;
    });
    function check(){
        // if ($("un").val() !== dn && $("pw").val() !== mk){
            alert("aaa");
};
</script>
</html>
